services:
  - type: web
    name: stocksync-demo
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: demo
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
