# StockSync Frontend

The frontend documentation lives in the repository root:

[Read the StockSync project README](../README.md)

This package is a React/Vite application. Run it locally with:

```bash
npm ci
npm run dev
```
